<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.what.multishop.mapper.IndexImgMapper">

    <resultMap id="BaseResultMap" type="com.spring.what.multishop.model.IndexImg">
            <id property="imgId" column="img_id" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="shopId" column="shop_id" />
            <result property="imgUrl" column="img_url" />
            <result property="status" column="status" />
            <result property="seq" column="seq" />
            <result property="spuId" column="spu_id" />
            <result property="imgType" column="img_type" />
    </resultMap>
    <select id="pageVO" resultType="com.spring.what.multishop.vo.IndexImgVO">
        select `img_id`,`img_url`,`status`,`seq`,`img_type` from index_img
        <where>
            <if test="indexImg.imgType != null">
                and img_type = #{indexImg.imgType}
            </if>
            <if test="indexImg.status != null">
                and `status` = #{indexImg.status}
            </if>
            <if test="indexImg.shopId != null">
                and `shop_id` = #{indexImg.shopId}
            </if>
        </where>
        order by seq desc,img_id desc
    </select>
    <select id="getList" resultType="com.spring.what.multishop.vo.IndexImgVO">
        select `img_id`, `img_url`, `status`, `seq`, `img_type`
        from index_img
        where shop_id = #{shopId}
        order by seq desc, img_id desc
    </select>
</mapper>
