<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.what.multishop.mapper.HotSearchMapper">

    <resultMap id="BaseResultMap" type="com.spring.what.multishop.model.HotSearch">
        <id property="hotSearchId" column="hot_search_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="shopId" column="shop_id"/>
        <result property="content" column="content"/>
        <result property="seq" column="seq"/>
        <result property="status" column="status"/>
        <result property="title" column="title"/>
    </resultMap>

    <select id="getPage" resultType="com.spring.what.multishop.vo.HotSearchVO">
        select hot_search_id, shop_id, content, seq, `status`, title
        from hot_search
        <where>
            <if test="e.shopId != null ">
                and shop_id = #{e.shopId}
            </if>
            <if test="e.status != null ">
                and status = #{e.status}
            </if>
            <if test="e.title != null ">
                and title like concat('%',#{e.title}, '%')
            </if>
            <if test="e.content != null ">
                and content like concat('%',#{e.content}, '%')
            </if>
        </where>
    </select>
    <select id="list" resultType="com.spring.what.multishop.vo.HotSearchVO">
        select hot_search_id, shop_id, content, seq, `status`, title
        from hot_search
        where shop_id = #{shopId}
    </select>
</mapper>
