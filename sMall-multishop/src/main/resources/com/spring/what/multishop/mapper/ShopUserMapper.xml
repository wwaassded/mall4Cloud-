<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.what.multishop.mapper.ShopUserMapper">

    <resultMap id="BaseResultMap" type="com.spring.what.multishop.model.ShopUser">
        <id property="shopUserId" column="shop_user_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="shopId" column="shop_id"/>
        <result property="nickName" column="nick_name"/>
        <result property="code" column="code"/>
        <result property="phoneNum" column="phone_num"/>
        <result property="hasAccount" column="has_account"/>
    </resultMap>
    <select id="getByShopUserId" resultType="com.spring.what.multishop.vo.ShopUserVO">
        select shop_user_id,
               shop_id,
               nick_name,
               `code`,
               phone_num,
               has_account
        from shop_user
        where shop_user_id = #{shopuserid}
    </select>
    <select id="getAdminUser" resultType="com.spring.what.multishop.vo.ShopUserVO">
        select shop_user_id,
               shop_id,
               nick_name,
               `code`,
               phone_num,
               has_account
        from shop_user
        where shop_id = #{shopId}
        order by create_time desc
        limit 1
    </select>
</mapper>
